name: tf.nn.scale_regularization_loss
dependency: 
  - regularization_loss

constraints:
  regularization_loss:
    descp: Regularization loss.
    dtype:
      - tf.tensor
    structure:
      - shape: Not specified

returns:
  descp: Scalar loss value.
  dtype:
    - tf.tensor
  structure:
    - shape: Scalar

errors:
  - RuntimeError: If the function is called in a cross-replica context rather than the expected replica context.

usage:
  descp: |
    Usage with distribution strategy and custom training loop:
    ```python
    with strategy.scope():
      def compute_loss(self, label, predictions):
        per_example_loss = tf.keras.losses.sparse_categorical_crossentropy(
            labels, predictions)
        loss = tf.nn.compute_average_loss(
            per_example_loss,
            sample_weight=sample_weight,
            global_batch_size=GLOBAL_BATCH_SIZE)
        loss += tf.nn.scale_regularization_loss(tf.nn.l2_loss(weights))
        return loss
    ```
