name: tf.math.real
needed:
  - input
constraints:
  input:
    descp: Must have numeric type. Returns the real part of each element in `input` considered as a complex number.
    dtype:
      - numeric
    structure:
      - tf.tensor
  name:
    descp: A name for the operation (optional).
    dtype:
      - string
    structure:
      - none
returns:
  descp: A Tensor of type float32 or float64, representing the real parts.
  dtype:
    - float32
    - float64
  structure:
    - tf.tensor
