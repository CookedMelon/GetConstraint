name: keras.layers.UpSampling2D
dependency:
  - size
  - data_format
  - interpolation

constraints:
  size:
    descp: Int, or tuple of 2 integers. The upsampling factors for rows and columns.
    dtype:
      - int
      - tuple
    structure:
      - shape: 2

  data_format:
    default: 'channels_last'
    descp: A string, one of `channels_last` (default) or `channels_first`. The ordering of the dimensions in the inputs.
    dtype:
      - string

  interpolation:
    default: 'nearest'
    descp: A string, one of `"area"`, `"bicubic"`, `"bilinear"`, `"gaussian"`, `"lanczos3"`, `"lanczos5"`, `"mitchellcubic"`, `"nearest"`.
    dtype:
      - string

returns:
  descp: 4D tensor with shape depends on `data_format`. If `data_format` is `"channels_last"`, the shape is `(batch_size, upsampled_rows, upsampled_cols, channels)`. If `data_format` is `"channels_first"`, the shape is `(batch_size, channels, upsampled_rows, upsampled_cols)`.
  dtype:
    - tf.tensor
  structure:
    - shape: ['batch_size', 'upsampled_rows', 'upsampled_cols', 'channels'] => data_format == 'channels_last'
    - shape: ['batch_size', 'channels', 'upsampled_rows', 'upsampled_cols'] => data_format == 'channels_first'
